// styles.js
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  channelListContainer: {
    flex: 1,
    backgroundColor: "#f5f5f5", // Exemple de couleur de fond
  },
  channelListItem: {
    padding: 10,
    borderBottomWidth: 1,
    borderBottomColor: "#ddd", // Exemple de couleur de bordure
  },
  channelListItemText: {
    fontSize: 16,
    color: "#333", // Exemple de couleur de texte
  },
});

export default styles;
